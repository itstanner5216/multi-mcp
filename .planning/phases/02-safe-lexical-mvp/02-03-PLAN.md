---
phase: 02-safe-lexical-mvp
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/multimcp/retrieval/logging.py
  - src/multimcp/retrieval/pipeline.py
  - src/multimcp/mcp_proxy.py
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - FALLBACK-01
  - FALLBACK-02

must_haves:
  truths:
    - "FileRetrievalLogger writes one JSONL line per call to log_ranking_event()"
    - "Each JSONL line contains all RankingEvent fields: session_id, turn_number, active_k, fallback_tier, router_enum_size, scorer_latency_ms"
    - "get_tools_for_list() returns at most config.max_k (default 20) direct tools plus the routing tool"
    - "Tier 6 fallback returns top-30 static defaults from sorted tool_to_server — never the full registry"
    - "_call_tool() dispatches to handle_routing_call() when tool_name == ROUTING_TOOL_KEY"
    - "All existing tests still pass"
  artifacts:
    - path: "src/multimcp/retrieval/logging.py"
      provides: "FileRetrievalLogger + log_ranking_event on ABC and NullLogger"
      contains: "FileRetrievalLogger"
    - path: "src/multimcp/retrieval/pipeline.py"
      provides: "Bounded K enforcement, routing tool assembly, RankingEvent emission"
      contains: "RankingEvent"
    - path: "src/multimcp/mcp_proxy.py"
      provides: "ROUTING_TOOL_KEY dispatch in _call_tool()"
      contains: "ROUTING_TOOL_KEY"
  key_links:
    - from: "src/multimcp/retrieval/pipeline.py"
      to: "src/multimcp/retrieval/logging.py"
      via: "pipeline calls logger.log_ranking_event(event)"
      pattern: "log_ranking_event"
    - from: "src/multimcp/retrieval/pipeline.py"
      to: "src/multimcp/retrieval/routing_tool.py"
      via: "pipeline calls build_routing_tool_schema(demoted_ids) and passes to assembler"
      pattern: "build_routing_tool_schema"
    - from: "src/multimcp/mcp_proxy.py"
      to: "src/multimcp/retrieval/routing_tool.py"
      via: "_call_tool() checks ROUTING_TOOL_KEY and dispatches to handle_routing_call()"
      pattern: "ROUTING_TOOL_KEY"
---

<objective>
Wire the full pipeline: FileRetrievalLogger for per-turn JSONL observability, updated pipeline that enforces the bounded active set with routing tool and fallback chain, and mcp_proxy integration that dispatches routing tool calls.

Purpose: After this plan, the ≤20 direct tool invariant is enforced end-to-end. Session init returns bounded tool list plus routing tool. Every turn emits a RankingEvent JSONL line. Model calls to request_tool are dispatched correctly.

Output: Updated logging.py, pipeline.py, mcp_proxy.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/multimcp/retrieval/logging.py
@src/multimcp/retrieval/pipeline.py
@src/multimcp/retrieval/models.py
@src/multimcp/retrieval/assembler.py
@src/multimcp/mcp_proxy.py
@.planning/phases/02-safe-lexical-mvp/02-01-SUMMARY.md
@.planning/phases/02-safe-lexical-mvp/02-02-SUMMARY.md
</context>

<interfaces>
From src/multimcp/retrieval/logging.py (current — no log_ranking_event yet):
  RetrievalLogger ABC has: log_retrieval, log_retrieval_miss, log_tool_sequence
  Need to add 4th abstract method: log_ranking_event(event: RankingEvent)
  NullLogger needs a no-op stub.
  New FileRetrievalLogger must implement all 4 methods.

From src/multimcp/retrieval/models.py (RankingEvent — already defined):
  @dataclass class RankingEvent: session_id, turn_number, catalog_version, workspace_hash,
    alpha, active_k, fallback_tier, active_tool_ids, router_enum_size, scorer_latency_ms, ...

From src/multimcp/retrieval/pipeline.py (existing get_tools_for_list):
  When enabled+ranker+assembler wired: calls assembler.assemble(ranked, config).
  Must add: (1) slice scored to config.max_k before assembling,
            (2) compute demoted_ids = all registry keys minus active top-K keys,
            (3) call build_routing_tool_schema(demoted_ids) and pass to assembler,
            (4) emit RankingEvent via await self.logger.log_ranking_event(event).

From src/multimcp/mcp_proxy.py (_call_tool at line 371):
  Currently dispatches tool_name to tool_to_server registry.
  Must add early check: if tool_name == ROUTING_TOOL_KEY, dispatch to routing tool handler.
  Routing tool handler: extract name/describe/arguments from req.params.arguments,
    call handle_routing_call(name, describe, arguments, self.tool_to_server),
    return types.ServerResult(content=result).
  If describe=False and proxy sentinel returned, forward to actual tool using name as tool_name.

From src/multimcp/retrieval/routing_tool.py (plan 02-02 output):
  ROUTING_TOOL_NAME = "request_tool"
  ROUTING_TOOL_KEY = "__routing__request_tool"
  build_routing_tool_schema(demoted_tool_ids: list[str]) -> types.Tool
  handle_routing_call(name, describe, arguments, tool_to_server) -> list[TextContent]
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FileRetrievalLogger to logging.py (OBS-01, OBS-02)</name>
  <files>src/multimcp/retrieval/logging.py</files>
  <read_first>
    - src/multimcp/retrieval/logging.py (current content — read before any edits)
    - src/multimcp/retrieval/models.py (RankingEvent dataclass — verify field names)
  </read_first>
  <behavior>
    - FileRetrievalLogger(log_path).log_ranking_event(event) appends one JSON line to file
    - Written line deserializes to dict containing keys: session_id, turn_number, active_k, fallback_tier, router_enum_size, scorer_latency_ms
    - Second call to log_ranking_event writes a second line
    - NullLogger.log_ranking_event(event) completes without error and writes nothing
    - RetrievalLogger ABC is abstract on log_ranking_event
  </behavior>
  <action>
Read src/multimcp/retrieval/logging.py before editing (it has RetrievalLogger ABC and NullLogger).

Add standard library imports at the top of the file: import dataclasses, json, pathlib

Add TYPE_CHECKING guard (after existing imports):
  from typing import TYPE_CHECKING (add to existing typing import if present)
  if TYPE_CHECKING:
      from .models import RankingEvent

Add 4th abstract method to RetrievalLogger class body (after log_tool_sequence):
  @abstractmethod
  async def log_ranking_event(self, event: "RankingEvent") -> None: ...

Add no-op implementation to NullLogger (after log_tool_sequence):
  async def log_ranking_event(self, event: "RankingEvent") -> None:
      pass

Append new class at end of file:

  class FileRetrievalLogger(RetrievalLogger):
      """Appends structured RankingEvents as JSONL. One line per call.
      Other abstract methods are no-ops; RankingEvent is the primary log unit."""

      def __init__(self, log_path: str) -> None:
          self._path = pathlib.Path(log_path)
          self._path.parent.mkdir(parents=True, exist_ok=True)

      async def log_ranking_event(self, event: "RankingEvent") -> None:
          line = json.dumps(dataclasses.asdict(event), default=str)
          with open(self._path, "a", encoding="utf-8") as f:
              f.write(line + "\n")

      async def log_retrieval(self, context, results, latency_ms: float) -> None:
          pass

      async def log_retrieval_miss(self, tool_name: str, context) -> None:
          pass

      async def log_tool_sequence(self, session_id: str, tool_a: str, tool_b: str) -> None:
          pass

Note: keep exact type annotations in log_retrieval/log_retrieval_miss matching existing ABC.
The dataclasses.asdict() call works because RankingEvent is a @dataclass.
  </action>
  <verify>
    <automated>cd /home/tanner/Projects/multi-mcp && grep -n "FileRetrievalLogger\|log_ranking_event" src/multimcp/retrieval/logging.py | head -10</automated>
  </verify>
  <done>
    - logging.py contains "class FileRetrievalLogger"
    - logging.py contains "log_ranking_event" in both ABC, NullLogger, and FileRetrievalLogger
    - grep -n "FileRetrievalLogger" src/multimcp/retrieval/logging.py returns at least 1 match
    - python -c "from src.multimcp.retrieval.logging import FileRetrievalLogger, NullLogger" exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update pipeline.py — bounded K, routing tool, fallback chain, RankingEvent (FALLBACK-01, FALLBACK-02, OBS-02)</name>
  <files>src/multimcp/retrieval/pipeline.py</files>
  <read_first>
    - src/multimcp/retrieval/pipeline.py (current content — read before editing)
    - src/multimcp/retrieval/models.py (RankingEvent, RetrievalConfig fields including max_k, enable_routing_tool)
    - src/multimcp/retrieval/routing_tool.py (build_routing_tool_schema, format_namespace_grouped signatures)
    - src/multimcp/retrieval/assembler.py (TieredAssembler.assemble new signature with routing_tool_schema)
    - src/multimcp/retrieval/logging.py (log_ranking_event — verify it exists after Task 1)
  </read_first>
  <behavior>
    - When config.enabled=False: returns all tools unchanged (existing behavior preserved)
    - When config.enabled=True and max_k=5: returns at most 5 active tools + routing tool
    - Demoted tool IDs = all tool_registry keys minus the active top-K keys
    - build_routing_tool_schema(demoted_ids) is called and passed to assembler.assemble()
    - After assembling, logger.log_ranking_event(event) is awaited with correct RankingEvent
    - RankingEvent.active_k == min(len(active_keys), config.max_k)
    - RankingEvent.router_enum_size == len(demoted_ids)
    - Fallback Tier 6: if all previous tiers fail or return empty, return sorted(tool_registry.keys())[:30] as tool list (conservative top-30 static defaults, never full registry)
    - config.enable_routing_tool=False: routing_tool_schema is NOT passed to assembler
  </behavior>
  <action>
Edit src/multimcp/retrieval/pipeline.py. Read the current file first.

Add imports at top of file (inside try/except or at top level as appropriate):
  from .models import RankingEvent  (add to existing models import line)
  Add to lazy imports block or at module level:
    from .routing_tool import build_routing_tool_schema, format_namespace_grouped, ROUTING_TOOL_KEY
  (Use try/except ImportError if routing_tool.py may not exist yet at import time.)

Rewrite get_tools_for_list() method. Keep the disabled path unchanged:
  if not self.config.enabled:
      return [m.tool for m in self.tool_registry.values()]

For the enabled path, replace/extend the existing logic:

  import time
  t0 = time.monotonic()

  # Ensure session exists
  self.session_manager.get_or_create_session(session_id)
  active_keys = self.session_manager.get_active_tools(session_id)

  # If no active keys or registry empty: fall through to fallback
  all_registry_keys = list(self.tool_registry.keys())

  if not active_keys:
      # Fallback Tier 1: score all tools with retriever if available
      # For Phase 2, seed with top-K from full registry sorted by key
      active_keys = set(all_registry_keys[:self.config.max_k])

  # Enforce max_k bound
  max_k = self.config.max_k  # default 20
  active_keys_list = sorted(active_keys)[:max_k]

  # Build active mappings
  active_mappings = [(k, self.tool_registry[k]) for k in active_keys_list if k in self.tool_registry]

  # Compute demoted IDs (all registry keys NOT in active set)
  active_key_set = {k for k, _ in active_mappings}
  demoted_ids = [k for k in all_registry_keys if k not in active_key_set]
  # Tier 6 bounded fallback: if active_mappings is empty, use top-30 static defaults
  if not active_mappings:
      fallback_keys = sorted(all_registry_keys)[:30]
      active_mappings = [(k, self.tool_registry[k]) for k in fallback_keys if k in self.tool_registry]
      demoted_ids = []

  # Build tool list
  scored_tools = [ScoredTool(tool_key=k, tool_mapping=m, score=1.0) for k, m in active_mappings]

  # Build routing tool schema (if enabled)
  routing_schema = None
  if self.config.enable_routing_tool and demoted_ids:
      routing_schema = build_routing_tool_schema(demoted_ids)

  # Assemble with ranker+assembler if wired, else raw fallback
  if self.ranker is not None and self.assembler is not None:
      ranked = self.ranker.rank(scored_tools)
      result = self.assembler.assemble(ranked, self.config, routing_tool_schema=routing_schema)
  else:
      result = [m.tool for _, m in active_mappings]
      if routing_schema is not None:
          result.append(routing_schema)

  latency_ms = (time.monotonic() - t0) * 1000.0

  # Emit RankingEvent (OBS-02)
  event = RankingEvent(
      session_id=session_id,
      turn_number=0,  # Turn tracking is Phase 3; use 0 for now
      catalog_version="",
      active_k=len(active_mappings),
      fallback_tier=1,
      active_tool_ids=[k for k, _ in active_mappings],
      router_enum_size=len(demoted_ids),
      scorer_latency_ms=latency_ms,
  )
  await self.logger.log_ranking_event(event)

  return result

Note: The existing on_tool_called and rebuild_catalog methods are unchanged.
  </action>
  <verify>
    <automated>cd /home/tanner/Projects/multi-mcp && grep -n "RankingEvent\|log_ranking_event\|build_routing_tool_schema\|demoted_ids" src/multimcp/retrieval/pipeline.py | head -10</automated>
  </verify>
  <done>
    - pipeline.py contains "RankingEvent" and "log_ranking_event"
    - pipeline.py contains "build_routing_tool_schema" or import of routing_tool
    - pipeline.py contains "demoted_ids"
    - pipeline.py contains "max_k" for K enforcement
    - grep -c "def get_tools_for_list" src/multimcp/retrieval/pipeline.py == 1 (method exists once)
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire routing tool dispatch in mcp_proxy.py (ROUTER-01)</name>
  <files>src/multimcp/mcp_proxy.py</files>
  <read_first>
    - src/multimcp/mcp_proxy.py (full file — read especially __init__, _call_tool at line 371, _list_tools at line 344)
    - src/multimcp/retrieval/routing_tool.py (ROUTING_TOOL_KEY, handle_routing_call signatures)
  </read_first>
  <action>
Edit src/multimcp/mcp_proxy.py. Read the file first. Make these surgical changes:

1. Add import near the top of _call_tool (or at module level in TYPE_CHECKING block):
   Use a lazy import inside the method to avoid circular imports:
   In _call_tool(), before the tool dispatch logic, add:

   try:
       from src.multimcp.retrieval.routing_tool import ROUTING_TOOL_KEY, handle_routing_call
       _has_routing = True
   except ImportError:
       _has_routing = False

   Then add early-return check at the start of _call_tool() body (right after extracting tool_name and arguments):

   if _has_routing and tool_name == ROUTING_TOOL_KEY:
       name = arguments.get("name", "")
       describe = bool(arguments.get("describe", False))
       call_args = arguments.get("arguments", {})
       content = handle_routing_call(name, describe, call_args, self.tool_to_server)
       if content and hasattr(content[0], 'text') and content[0].text.startswith("__PROXY_CALL__:"):
           # Proxy call: strip sentinel and forward to the actual tool
           actual_tool_name = content[0].text.split(":", 1)[1]
           # Recurse using actual_tool_name as if it were the original call
           from mcp import types as _types
           proxy_req = _types.CallToolRequest(
               method="tools/call",
               params=_types.CallToolRequestParams(name=actual_tool_name, arguments=call_args)
           )
           return await self._call_tool(proxy_req)
       return types.ServerResult(content=content)

2. The _list_tools method already delegates to retrieval_pipeline.get_tools_for_list() which now
   appends the routing tool via the assembler. No additional changes needed in _list_tools.

3. Verify: no changes to register_client, unregister_client, or other methods.

The ROUTING_TOOL_KEY value "__routing__request_tool" is intentionally NOT in tool_to_server,
so the normal tool dispatch path won't find it — the early-return check is the only handler.
  </action>
  <verify>
    <automated>cd /home/tanner/Projects/multi-mcp && grep -n "ROUTING_TOOL_KEY\|handle_routing_call" src/multimcp/mcp_proxy.py | head -5</automated>
  </verify>
  <done>
    - mcp_proxy.py contains "ROUTING_TOOL_KEY" reference
    - mcp_proxy.py contains "handle_routing_call" reference
    - grep "ROUTING_TOOL_KEY" src/multimcp/mcp_proxy.py returns at least 1 match
    - Existing tests pass: pytest tests/ -x -q exits 0
  </done>
</task>

</tasks>

<verification>
Full regression check:
  cd /home/tanner/Projects/multi-mcp && python -m pytest tests/ -x -q

Check ROUTING_TOOL_KEY wired:
  grep -n "ROUTING_TOOL_KEY" src/multimcp/mcp_proxy.py

Check FileRetrievalLogger importable:
  python -c "from src.multimcp.retrieval.logging import FileRetrievalLogger; print('OK')"

Check pipeline emits RankingEvent:
  grep -n "log_ranking_event\|RankingEvent" src/multimcp/retrieval/pipeline.py

Check fallback bounded (never full registry):
  grep -n "30\|fallback\|demoted_ids" src/multimcp/retrieval/pipeline.py
</verification>

<success_criteria>
1. grep "FileRetrievalLogger" src/multimcp/retrieval/logging.py returns a match
2. grep "log_ranking_event" src/multimcp/retrieval/pipeline.py returns a match
3. grep "ROUTING_TOOL_KEY" src/multimcp/mcp_proxy.py returns a match
4. pytest tests/ -x -q exits 0 (all existing tests pass, no regressions)
5. python -c "from src.multimcp.retrieval.logging import FileRetrievalLogger" exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-safe-lexical-mvp/02-03-SUMMARY.md`
</output>
