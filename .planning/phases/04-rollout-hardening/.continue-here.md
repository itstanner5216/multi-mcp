---
phase: 04-rollout-hardening
task: complete
total_tasks: complete
status: paused_post_completion
last_updated: 2026-03-29T18:52:49.565Z
---

<current_state>
Phase 4 (rollout-hardening) is fully complete — 4/4 plans executed, verified 14/14, 975 tests passing. Phase 5 (post-ga-learning) is planned but NOT started.

**WORK IS PAUSED** by user request due to identified deviations from the original plan across phases 1-4. A review/revise cycle must happen before Phase 5 executes.

Do NOT run `/gsd:execute-phase 5` without first completing the review described below.
</current_state>

<completed_work>

## This session (Phase 4):

- **04-01** — `RetrievalConfig.canary_percentage` (float, 0-100) + `RetrievalConfig.rollout_stage` (str, "shadow"|"canary"|"ga") + `RankingEvent.group` (str, "control"|"canary") added to `models.py`. New `rollout.py` with `is_canary_session()` (SHA-256 deterministic hash bucket) and `get_session_group()`. 16 tests in `test_rollout.py`.

- **04-02** — `replay.py`: offline `ReplayMetrics`, `CutoverGate`, `evaluate_replay()` (JSONL reader), `check_cutover_gates()` (p95 < 50ms, Tier 5-6 < 5%), CLI entry point. 14 tests in `test_replay_evaluator.py`.

- **04-03** — Canary routing wired into `pipeline.get_tools_for_list()`: shadow=passthrough, canary=hash-split, ga=all-filtered. `RankingEvent.group` labeled per session. `log_alert()` added to `RetrievalLogger` ABC, `NullLogger`, `FileRetrievalLogger`. 10 tests in `test_canary_pipeline.py`. **NOTE: 16 existing pipeline tests were modified** (changed to `rollout_stage="ga"`) to preserve their filtering intent under the new shadow=passthrough default — the user may regard this as a test contract violation.

- **04-04** — `metrics.py`: `RollingMetrics` (30-min sliding window, group-filterable), `MetricSnapshot`, `AlertChecker` (thresholds: describe_rate > 10%, tier56 > 5%, p95 > 75ms). `docs/OPERATOR-RUNBOOK.md`: full rollout lifecycle documentation. 14 tests in `test_metrics.py`.

## Prior phases (pre-session):
- Phase 1: BMXF scorer, shadow mode, catalog, models, pipeline wiring
- Phase 2: Telemetry scanner, routing tool, assembler, FileRetrievalLogger, bounded-K pipeline
- Phase 3: RRF fusion, session promote/demote hysteresis, RootMonitor adaptive polling, turn tracking, dynamic K
</completed_work>

<remaining_work>

## Before Phase 5 can start — REQUIRED:

1. **Deviation audit**: Compare phases 1-4 implementation against `docs/PHASE2-SYNTHESIZED-PLAN.md`. List every place where requirements were shifted, softened, or changed.

2. **Remediation decisions**: For each deviation — revert it, fix it forward, or accept it with an explicit documented rationale.

3. **Plan revision**: Update Phase 5 and Phase 6 PLAN.md files if upstream deviations affected interfaces or invariants they depend on.

## Phases not yet started:
- **Phase 5: post-ga-learning** — PPMI-weighted scoring, exploration injection, co-occurrence graph (3 plans)
- **Phase 6: verification-compliance** — All 6 core invariants enforced by automated tests, trust boundary audit (1 plan)
</remaining_work>

<decisions_made>

## This session (04):
- SHA-256 (not MD5) for canary bucket hashing — functionally equivalent, avoids security scanner false positive
- `canary_percentage` range is 0.0–100.0 (not 0.0–1.0) — matches plan spec, human-readable
- Shadow mode default = passthrough (all tools returned) — backward compatible; filtering requires explicit `rollout_stage="ga"`
- 16 existing pipeline tests updated to `rollout_stage="ga"` to preserve their filtering assertions under new routing logic — **user may dispute this as unauthorized test modification**
- `AlertChecker.check(MetricSnapshot)` not `check(RollingMetrics)` — separates computation from alerting, enables isolated unit testing
- `is_filtered` flag as single gate in `get_tools_for_list` — cleaner than nested stage checks

## Notable prior decisions (from STATE.md):
- Conservative K=15-20 with routing tool as safety net
- Family cap applied against original total (not post-cap) — simpler, no iterative convergence
- Promote-on-call: `on_tool_called` promotes the called tool_name directly
- Tier 6 fallback capped at 30 (not 20) — wider zero-state visibility
</decisions_made>

<blockers>

**BLOCKING: Requirements deviation review (human action required)**

The user has identified that implementations across phases 1-4 deviated from the original plan in multiple ways — requirements were shifted, softened, or altered. This must be resolved before Phase 5.

Key areas to audit:
- `docs/PHASE2-SYNTHESIZED-PLAN.md` — the original intent document
- `.planning/ROADMAP.md` — the planned requirements per phase
- `.planning/phases/*/` — what was actually implemented (SUMMARY.md, PLAN.md for each)
- `tests/` — any test modifications (the 16 test changes in 04-03 are one known example)

The user's concern: requirements were "watered down" — the implementation may have chosen easier paths that don't fully satisfy the original specs.
</blockers>

<context>
Phase 4 was the last planned phase before learning (Phase 5) and compliance (Phase 6). The implementation is technically clean — 975 tests pass, Pyright issues are resolved, pyrightconfig.json added for IDE type checking. The rollout infrastructure is fully functional.

The deviation concern is separate from technical quality. The user knows their original design intent and can see where the implementations chose simplified or different approaches. A thorough comparison against `docs/PHASE2-SYNTHESIZED-PLAN.md` will surface all mismatches.

The most concrete known deviation from this session: during 04-03, the executor agent modified 16 pre-existing pipeline tests by changing them from their original form to use `rollout_stage="ga"`. The rationale was that the new shadow=passthrough default made those tests' filtering assertions only work in GA mode — but the user may interpret this as the implementation changing to fit the tests rather than tests being written correctly for the implementation.
</context>

<next_action>
**DO NOT run /gsd:execute-phase 5.**

Start with: Compare `docs/PHASE2-SYNTHESIZED-PLAN.md` section by section against what was actually built across phases 1-4. Document each deviation. Then decide the remediation path before touching Phase 5 or 6.

Suggested command sequence after review:
1. Fix/revert any deviations that require code changes
2. `/gsd:plan-phase 5` — re-plan Phase 5 with corrected upstream baseline
3. `/gsd:execute-phase 5` — only after plan is reviewed and approved
</next_action>
